import 'package:flutter/material.dart';
import 'package:flutter_application_1/static.dart'; // Importa o arquivo que contém o widget principal do app

// Função principal que executa o aplicativo
void main() {
  runApp(const MyApp()); // Inicia o app chamando o widget MyApp
}
